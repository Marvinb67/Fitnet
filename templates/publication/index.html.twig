{% extends 'base.html.twig' %}

{% block title %}Hello PublicationController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Liste des publications</h1>
    {% for publication in publications %}
        <div>
            <p>{{publication.user.nom}} {{publication.user.prenom}}</p>
            <h2><a href="{{ path('app_publication_show', {id: publication.id, slug: publication.slug})}}">{{publication.titre}}</a></h2>
            <p>{{publication.contenu}}</p>
            <p>Posté le : {{publication.createdAt | date('Y/m/d')}}</p>
            <a href="{{ path('app_publication_edit', {id: publication.id})}}">Modifier la publication</a> <a href="{{ path('app_publication_delete', {id: publication.id})}}">Supprimer la publication</a>
        </div>
        <hr>
    {% endfor %}
</div>
{% endblock %}
