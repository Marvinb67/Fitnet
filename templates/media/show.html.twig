{% extends 'base.html.twig' %}

{% block title %}{{media.titre}}{% endblock %}

{% block container %}
<section class="container my-3">
    <div class="row">
        <div class="col-12">
            <h1>{{media.titre}}</h1>
            <img class="card-img-top img-fluid" src="{{media.lien}}" alt="{{media.titre}}"></a>
            <p>{{ media.description }}</p>
        </div>
    </div>
</section>

{% endblock %}